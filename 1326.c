#include <string.h>

int nt[10001][100];

int minTaps(int n, int* ranges, int rangesSize){
  memset(nt, 0, sizeof(nt));

  for (int j = 0; j < rangesSize; j++) {
    for (int i = 0, r = ranges[j]; i <= r; i++) {

    }
  }
}

int main() {

}
